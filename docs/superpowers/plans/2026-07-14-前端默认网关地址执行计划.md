# 前端默认网关地址执行计划

> **执行约束：**按任务逐项执行。每项先写失败测试，再写最小实现，再运行验证。

**目标：**让本地前端默认请求本机网关，并在收到网页内容等非接口响应时显示准确错误提示。

**架构：**运行时模块固定提供本机网关基础地址和统一响应解析函数。登录页与注册页继续负责各自表单流程，但不再自行把响应直接解析为结构化数据。

**技术栈：**前端框架、前端构建工具、原生请求接口、节点测试运行器。

---

## 任务一：为默认网关地址和响应解析建立测试

**文件：**

- 修改：`front/src/utils/runtime.test.mjs`
- 测试：`front/src/utils/runtime.test.mjs`

- [ ] **步骤一：新增默认网关地址测试。**

```js
import { apiBase, normalizeApiBase, parseAPIResponse } from "./runtime.js";

test("默认接口地址指向本机网关", () => {
  assert.equal(apiBase, "http://127.0.0.1:8080");
});
```

- [ ] **步骤二：新增非结构化响应测试。**

```js
test("非结构化响应返回明确的网关地址错误", async () => {
  const response = new Response("<html>开发服务器页面</html>", {
    headers: { "content-type": "text/html; charset=utf-8" },
  });

  await assert.rejects(
    () => parseAPIResponse(response),
    /网关返回了非接口数据，请检查网关地址/
  );
});
```

- [ ] **步骤三：新增有效结构化响应测试。**

```js
test("结构化响应返回解析后的数据", async () => {
  const response = new Response(JSON.stringify({ code: 0, msg: "ok" }), {
    headers: { "content-type": "application/json; charset=utf-8" },
  });

  assert.deepEqual(await parseAPIResponse(response), { code: 0, msg: "ok" });
});
```

- [ ] **步骤四：运行测试并确认当前实现失败。**

```powershell
node --test src/utils/runtime.test.mjs
```

预期：默认网关地址测试失败，响应解析函数导入失败。

## 任务二：实现固定默认网关和统一解析函数

**文件：**

- 修改：`front/src/utils/runtime.js`
- 测试：`front/src/utils/runtime.test.mjs`

- [ ] **步骤一：将接口基础地址固定为本机网关地址。**

```js
export const apiBase = normalizeApiBase("http://127.0.0.1:8080");
```

删除构建环境变量读取逻辑。

- [ ] **步骤二：新增统一响应解析函数。**

```js
export async function parseAPIResponse(response) {
  const contentType = String(response?.headers?.get?.("content-type") || "").toLowerCase();
  if (!contentType.includes("application/json")) {
    throw new Error("网关返回了非接口数据，请检查网关地址");
  }
  try {
    return await response.json();
  } catch {
    throw new Error("网关返回了无效接口数据，请稍后重试");
  }
}
```

- [ ] **步骤三：运行运行时模块测试。**

```powershell
node --test src/utils/runtime.test.mjs
```

预期：全部通过。

## 任务三：让登录和注册页复用统一响应解析

**文件：**

- 修改：`front/src/pages/login/index.vue`
- 修改：`front/src/pages/register/index.vue`

- [ ] **步骤一：登录页导入并使用统一响应解析函数。**

```js
import { apiBase, parseAPIResponse } from "../../utils/runtime.js";

const result = await parseAPIResponse(response);
```

异常提示改为：

```js
toast(error?.message || "网关服务暂不可用");
```

- [ ] **步骤二：注册页在发送验证码、注册和自动登录三个请求中使用统一解析函数。**

```js
const result = await parseAPIResponse(response);
```

异常提示改为：

```js
toast(error?.message || "网关服务暂不可用");
```

- [ ] **步骤三：运行前端全部工具测试。**

```powershell
node --test src/utils/*.test.mjs
```

预期：全部通过。

- [ ] **步骤四：提交前端实现与测试。**

```powershell
git add front/src/utils/runtime.js front/src/utils/runtime.test.mjs front/src/pages/login/index.vue front/src/pages/register/index.vue
git commit -m "fix: 默认请求本地网关"
```

## 任务四：构建验证

**文件：**

- 验证：前端浏览器构建

- [ ] **步骤一：构建前端。**

```powershell
npm.cmd run build:h5
```

预期：构建成功。

- [ ] **步骤二：确认工作区状态。**

```powershell
git status --short --branch
```

预期：真实本地配置文件不出现，分支只包含前端运行时、页面、测试和设计文档改动。
