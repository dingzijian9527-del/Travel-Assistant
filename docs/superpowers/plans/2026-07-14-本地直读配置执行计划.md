# 本地直读配置执行计划

> **执行约束：**按任务逐项执行。每项先写失败测试，再写最小实现，再运行验证。

**目标：**取消配置路径和配置字段的环境变量依赖，让开发者在项目根目录直接启动服务时自动读取本地真实配置文件。

**架构：**配置加载函数统一负责配置文件选择：显式路径优先，其次是本地配置文件，最后是示例配置文件。启动装配只调用统一加载函数，不再读取环境变量。真实值继续保存在被忽略的本地配置文件中。

**技术栈：**后端语言、配置库、单元测试、命令行启动验证。

---

## 任务一：为本地配置选择规则建立测试

**文件：**

- 修改：`pkg/config/config_test.go`
- 测试：`pkg/config/config_test.go`

- [ ] **步骤一：新增本地配置优先测试。**

```go
func TestLoadUsesLocalConfigBeforeExampleConfig(t *testing.T) {
    root := t.TempDir()
    confDir := filepath.Join(root, "conf")
    if err := os.MkdirAll(confDir, 0o755); err != nil {
        t.Fatal(err)
    }
    if err := os.WriteFile(filepath.Join(confDir, "config.yaml"), []byte("auth:\n  jwt_secret: example-secret\n"), 0o600); err != nil {
        t.Fatal(err)
    }
    if err := os.WriteFile(filepath.Join(confDir, "config.local.yaml"), []byte("auth:\n  jwt_secret: local-secret\n"), 0o600); err != nil {
        t.Fatal(err)
    }
    t.Chdir(root)

    cfg, err := Load("")
    if err != nil {
        t.Fatalf("load config failed: %v", err)
    }
    if cfg.Auth.JWTSecret != "local-secret" {
        t.Fatalf("jwt secret = %q, want local-secret", cfg.Auth.JWTSecret)
    }
}
```

- [ ] **步骤二：新增显式路径优先、错误本地配置不回退、环境变量不覆盖文件值测试。**

```go
func TestLoadExplicitPathHasHighestPriority(t *testing.T) {
    root := t.TempDir()
    confDir := filepath.Join(root, "conf")
    if err := os.MkdirAll(confDir, 0o755); err != nil {
        t.Fatal(err)
    }
    localPath := filepath.Join(confDir, "config.local.yaml")
    explicitPath := filepath.Join(root, "explicit.yaml")
    if err := os.WriteFile(localPath, []byte("auth:\n  jwt_secret: local-secret\n"), 0o600); err != nil {
        t.Fatal(err)
    }
    if err := os.WriteFile(explicitPath, []byte("auth:\n  jwt_secret: explicit-secret\n"), 0o600); err != nil {
        t.Fatal(err)
    }
    t.Chdir(root)

    cfg, err := Load(explicitPath)
    if err != nil {
        t.Fatalf("load config failed: %v", err)
    }
    if cfg.Auth.JWTSecret != "explicit-secret" {
        t.Fatalf("jwt secret = %q, want explicit-secret", cfg.Auth.JWTSecret)
    }
}

func TestLoadReturnsErrorForInvalidLocalConfig(t *testing.T) {
    root := t.TempDir()
    confDir := filepath.Join(root, "conf")
    if err := os.MkdirAll(confDir, 0o755); err != nil {
        t.Fatal(err)
    }
    if err := os.WriteFile(filepath.Join(confDir, "config.yaml"), []byte("auth:\n  jwt_secret: example-secret\n"), 0o600); err != nil {
        t.Fatal(err)
    }
    if err := os.WriteFile(filepath.Join(confDir, "config.local.yaml"), []byte("auth: [\n"), 0o600); err != nil {
        t.Fatal(err)
    }
    t.Chdir(root)

    if _, err := Load(""); err == nil {
        t.Fatal("invalid local config should return an error")
    }
}

func TestLoadDoesNotOverrideFileValueFromEnvironment(t *testing.T) {
    configPath := filepath.Join(t.TempDir(), "config.yaml")
    if err := os.WriteFile(configPath, []byte("auth:\n  jwt_secret: file-secret\n"), 0o600); err != nil {
        t.Fatal(err)
    }
    t.Setenv("TRAVEL_ASSISTANT_AUTH_JWT_SECRET", "environment-secret")

    cfg, err := Load(configPath)
    if err != nil {
        t.Fatalf("load config failed: %v", err)
    }
    if cfg.Auth.JWTSecret != "file-secret" {
        t.Fatalf("jwt secret = %q, want file-secret", cfg.Auth.JWTSecret)
    }
}
```

- [ ] **步骤三：运行新增测试并确认当前实现失败。**

```powershell
go test ./pkg/config -run "TestLoadUsesLocalConfigBeforeExampleConfig|TestLoadExplicitPathHasHighestPriority|TestLoadReturnsErrorForInvalidLocalConfig|TestLoadDoesNotOverrideFileValueFromEnvironment"
```

预期：本地配置优先测试失败；环境变量不覆盖测试失败，证明现有行为仍依赖环境变量。

## 任务二：实现无环境变量的配置文件选择

**文件：**

- 修改：`pkg/config/config.go`
- 修改：`pkg/bootstrap/bootstrap.go`
- 测试：`pkg/config/config_test.go`

- [ ] **步骤一：在配置模块新增默认路径选择函数。**

```go
func resolveConfigPath(path string) string {
    if explicit := strings.TrimSpace(path); explicit != "" {
        return explicit
    }
    localPath := filepath.Join("conf", "config.local.yaml")
    if info, err := os.Stat(localPath); err == nil && !info.IsDir() {
        return localPath
    }
    return filepath.Join("conf", "config.yaml")
}
```

- [ ] **步骤二：让加载函数只读取选中的配置文件，并删除环境变量覆盖逻辑。**

```go
configPath := resolveConfigPath(path)
v.SetConfigFile(configPath)
if err := v.ReadInConfig(); err != nil {
    return nil, err
}
```

删除配置路径搜索、环境变量前缀、环境变量字段替换和自动环境变量读取。

- [ ] **步骤三：让统一启动装配不再读取配置路径环境变量。**

```go
func Init(service string) (*Runtime, error) {
    return InitWithConfigPath(service, "")
}
```

删除启动装配中与配置路径环境变量和默认配置路径变量有关的初始化代码。

- [ ] **步骤四：运行配置与启动装配测试。**

```powershell
go test ./pkg/config ./pkg/bootstrap
```

预期：全部通过。

- [ ] **步骤五：提交配置加载与测试改动。**

```powershell
git add pkg/config/config.go pkg/config/config_test.go pkg/bootstrap/bootstrap.go
git commit -m "feat: 自动读取本地配置文件"
```

## 任务三：改写模板与开发说明

**文件：**

- 修改：`conf/config.yaml.tpl`
- 修改：`README.md`
- 修改：`docs/代码用法与说明.md`

- [ ] **步骤一：将模板中的环境变量占位值改为空白直接配置值。**

```yaml
mysql:
  dsn: ""

auth:
  jwt_secret: ""
  jwt_expire: 24h
  jwt_refresh_expire: 168h
```

其他密码、密钥、地址字段也改为直接填写的空字符串，不保留环境变量占位语法。

- [ ] **步骤二：更新启动说明。**

说明复制示例配置为本地配置文件并直接填写真实值后，使用下列命令启动：

```powershell
go run .\cmd\user
go run .\cmd\trip
go run .\cmd\ai-agent
go run .\cmd\api
```

明确说明本地配置文件优先级、被忽略规则和示例配置不可直接启动的原因。

- [ ] **步骤三：检查模板中不存在环境变量占位语法。**

```powershell
Select-String -LiteralPath conf\config.yaml.tpl -Pattern '\$\{'
```

预期：无匹配结果。

- [ ] **步骤四：提交模板与说明改动。**

```powershell
git add conf/config.yaml.tpl README.md docs/代码用法与说明.md
git commit -m "docs: 改为本地直读配置说明"
```

## 任务四：清理旧环境变量并验证简洁启动

**文件：**

- 修改：用户级配置路径环境变量
- 不提交：`conf/config.local.yaml`

- [ ] **步骤一：删除旧的用户级配置路径环境变量。**

```powershell
[Environment]::SetEnvironmentVariable("TRAVEL_ASSISTANT_CONFIG", $null, "User")
```

- [ ] **步骤二：执行全量测试与前端构建。**

```powershell
go test ./...
node --test src/utils/*.test.mjs
npm.cmd run build:h5
```

预期：全部通过。

- [ ] **步骤三：在未设置配置环境变量的新进程中启动用户服务。**

```powershell
Remove-Item Env:TRAVEL_ASSISTANT_CONFIG -ErrorAction SilentlyContinue
go run .\cmd\user
```

预期：不再出现示例令牌密钥错误，服务成功监听用户服务端口。

- [ ] **步骤四：停止验证服务并确认工作区状态。**

```powershell
git status --short --branch
```

预期：仅出现已忽略的本地配置文件，不出现真实密钥或数据库密码。

