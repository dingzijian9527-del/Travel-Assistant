规则名称：项目开发约束
适用范围：本仓库后续所有服务端开发、目录创建、代码生成、代码审查和交付验收。

框架约束：
1. 后端服务必须使用 Kitex（Go 微服务远程过程调用框架）脚手架生成和维护。
2. 接口定义文件变更后，必须通过 Kitex（Go 微服务远程过程调用框架）命令重新生成框架代码。
3. 不允许手写、移动、拆分或改名 Kitex（Go 微服务远程过程调用框架）生成代码。
4. 业务实现优先放在脚手架生成的处理器文件中；确需拆分时，必须先更新本规则并说明目录职责。

目录约束：
项目目录必须严格参照 CloudWeGo（云织）官方 Kitex（Go 微服务远程过程调用框架）示例 hello 目录。

允许目录：
client
conf
kitex_gen
script

允许根目录文件：
*.thrift
*.go
*.sh
go.mod
go.sum
README.md
AGENTS.md

标准目录形态：
.
├── client
├── conf
├── kitex_gen
│   └── api
├── script
├── build.sh
├── handler.go
├── hello.thrift
├── main.go
├── run.sh
└── test.sh

禁止事项：
1. 不允许自行新增 app、cmd、internal、pkg、service、api、config、docs、deploy、scripts 等未在官方示例中出现的业务目录。
2. 不允许为了分层、整洁或预留扩展而创建空目录、占位目录或暂时无代码引用的目录。
3. 不允许把 Kitex（Go 微服务远程过程调用框架）生成目录 kitex_gen 改名、移动或拆分。
4. 不允许把脚手架生成的入口文件、处理器文件和接口定义文件移动到自定义目录。

例外流程：
1. 如确实需要新增官方示例之外的目录，必须先在本规则中新增目录名称、职责边界和必要性说明。
2. 新增目录必须在代码变更前获得明确确认。
3. 新目录必须在同一次变更中被真实代码使用，不允许只创建目录本身。

交付验收：
1. 根目录结构必须符合本规则。
2. 新增目录必须全部在允许清单内，或已按例外流程更新本规则。
3. Kitex（Go 微服务远程过程调用框架）生成代码只能位于 kitex_gen。
4. 接口定义变更后必须重新运行代码生成。

参考来源：
CloudWeGo（云织）官方 Kitex（Go 微服务远程过程调用框架）入门示例说明：https://www.cloudwego.io/docs/kitex/getting-started/quick_start/
CloudWeGo（云织）官方 Kitex（Go 微服务远程过程调用框架）示例仓库 hello 目录：https://github.com/cloudwego/kitex-examples/tree/main/hello
